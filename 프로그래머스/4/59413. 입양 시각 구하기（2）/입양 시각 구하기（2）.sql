WITH RECURSIVE HOUR_CNT as (
    SELECT 0 HOUR
    
    UNION ALL
    
    SELECT HOUR+1
    FROM HOUR_CNT
    WHERE HOUR < 23
)

SELECT HOUR
    , COUNT(ANIMAL_ID) COUNT
FROM HOUR_CNT h
LEFT JOIN ANIMAL_OUTS a ON HOUR(a.DATETIME) = h.HOUR
GROUP BY HOUR
ORDER BY HOUR